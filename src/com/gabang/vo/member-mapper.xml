<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gabang.vo.member-mapper">
	<select id="loginIdCheck" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM member WHERE email=#{email}
	</select>
	
	<select id="loginPwdCheck" parameterType="String" resultType="MemberVO">
		SELECT pwd,nick FROM member WHERE email=#{email}
	</select>

</mapper>